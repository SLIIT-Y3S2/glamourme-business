import 'package:flutter/material.dart';

class AddbusinessScreen2 extends StatelessWidget {
  const AddbusinessScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Business Screen')),
      body: Text("add business screen 2"),
    );
  }
}